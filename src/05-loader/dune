(library
 (name loader)
 (libraries language typechecker parser))

(rule
 (with-stdout-to
  stdlib_eff.ml
  (progn
   (echo "let stdlib = {|")
   (cat stdlib.eff)
   (echo "|}")
   (echo "let multicoreStdlib = {|")
   (cat ../04-backends/multicore/stdlib.eff)
   (echo "|}"))))
