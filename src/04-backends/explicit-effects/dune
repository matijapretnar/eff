(library
 (name explicitEffects)
 (libraries utils language typechecker))

(rule
 (with-stdout-to
  ocamlHeader_ml.ml
  (progn
   (echo "let source = {|")
   (cat ocamlHeader.ml)
   (echo "|}"))))

(env
 (dev
  (flags
   (:standard -warn-error -A))))
