(** Overview *)
let x = 10+10
let y = x * 3
let rec fact n = if n = 0 then 1 else n * fact (n - 1)

(** Mutually recursive function *)
let rec even n =
  match n with
  | 0 -> true
  | x -> odd (x-1)
and odd n =
  match n with
  | 0 -> false
  | x -> even (x-1)
